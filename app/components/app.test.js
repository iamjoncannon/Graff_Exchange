


describe("app", ()=>{

    it('1) renders successfully with data from redux store', ()=>{

        // let tree = component.toJSON();
        
        // expect(tree).toMatchSnapshot();
    })

    it('if props.location.pathname === "/", renders landing and footer components inside "app-container" div', ()=>{


    })

    it('if not logged in, Redirects to "/"', ()=>{})
    it('>> if logged in <<', ()=>{})
    it('if path includes "/indiv", renders MainNav and IndivNav components', ()=>{})
    it('if path includes "/all", renders MainNav and AllNav components', ()=>{})
})