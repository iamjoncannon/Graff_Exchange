

/*

User_actions.LOGIN

if invalid token, sets state.token_error to true 

if other server error, returns state

converts received data to object where key is symbol and value is holding object

selects first symbol as selectedPortfolioItem

actions.HYDRATEPORTFOLIO

if other server error, returns state

converts received data to object where key is symbol and value is holding object

selects first symbol as selectedPortfolioItem

actions.MAKETRADE



*/